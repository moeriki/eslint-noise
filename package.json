{
  "name": "eslint-noise",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": "^10 || ^12 || ^14"
  },
  "scripts": {
    "lint": "eslint --cache --fix packages/",
    "test": "eslint packages/ && jest"
  },
  "devDependencies": {
    "eslint": "^7.15.0",
    "jest": "^26.6.3",
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
