{
  "name": "eslint-noise",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": "^10 || ^12 || ^14"
  },
  "scripts": {
    "lint": "eslint --cache --fix packages/",
    "test": "jest"
  },
  "devDependencies": {
    "eslint": "^7.9.0",
    "jest": "^26.4.2",
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
